导包操作：
快捷键导包 alt+enter / 自动导包 下面写，自动知道你需要什么包
不死神兔：兔子生兔子是斐波那契数列
求和，用itar，soutv
查找小demo：如果没有，输出-1，表示不存在
Shift+ 小键盘上面的1  或者 小键盘1
万物皆对象。 面向对象，人关注着对象，人关注具体事务和信息，  小米手机是对象/hhh小米。我粉了！
关注手机的各种信息。面向对象操作， 什么样的手机 
类是对象的数据类型，类是对象的数据类型，
属性：有特定的值 行为：对象执行的操作
类：是一类事物的抽象 对象：看得见摸得着的真实存在的实体  类 ：手机类  对象： 小米手机 有属性、行为
类也是由属性和行为构成。属性：成员变量（默认值是null/0），行为：成员方法。//public class 类名{}
创建对象： //类名 对象名 = new 类名（）
使用对象：使用成员变量//p.brand。使用成员方法//p.call()
创建和使用对象，就是测试类，就应该有一个main方法。sout属性/直接方法：p.call();
对象内存图：单个对象 修改了堆内存。 /内存图对应，栈内存和堆内存
多个对象，就是new一个新的堆内存地址了！成员方法是可以共用的，调用者不同。成员变量不能共用。
但设计一个多个对象指向相同。方法main出现在栈内存，new Student（）出现在堆内存。
成员变量和局部变量：类中方法外/方法内或方法声明上  堆内存/栈内存 随对象/随方法调用 
封装：private （对于类中的成员变量）提供get/set方法。  
标准学生类 使用set方法给成员变量赋值。 使用get方法获取成员变量的值。
在方法中写name，只能看到局部变量，this.name 就可以表示成员变量了。方法被哪个对象引用，this就代表哪个对象（如：s1 s2)
封装，继承和多态。
隐藏信息不能直接访问，只能通过我们提供的方法进行访问。封装，安全性和复用性
构造方法：public Student(){ 分别给一个对象赋值。Student s = new Student();为了给（）里面写值，用构造方法//精辟
/多个构造方法，也是重载的形式。写构造方法，都要写
