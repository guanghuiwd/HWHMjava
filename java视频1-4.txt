类中包含：成员变量/构造方法/成员方法

API 应用程序编程接口
使用帮助文档，API
调用方法时，自动补全接受返回值：快捷键符合逻辑，CTRL + alt + V
String
java.lang包 说明不用导包。String创建后不能更改。
String中的构造方法，成员方法
char是字符数组// {'a','b',} 这个逗号jdk12也是不报错的。
byte是字节数组。//一个字节（byte）＝8位（bit），“byte数组”里面全部是“byte”，即每一个byte都可以用二进制、十六进制、十进制来表示。//数字其实对应ASCII码值
